{{>partials/header}}

<main class="main">
    <h2 class="main__title">Pacientes no registro</h2>
    <!-- Dados dos usuários -->
    <section class="user-area">
        {{#patients}}
        <div class="user-card">
            <h3 class="user-card__name">Nome Completo<br>{{ name }}</h3>
            <p class="user-card__info">Data de Nascimento: {{ dateBirth }}</p>
            <p class="user-card__info">Número do SUS: {{ numSus }}</p>
            <p class="user-card__info">Histórico de Doença: Não obtido</p>
        </div>

        <div class="user-card">
            <!-- Dados do segundo usuário -->
        </div>
        {{/patients}}
        <!-- Repita as divs para cada usuário -->
    </section>
</main>

{{>partials/footer}}
